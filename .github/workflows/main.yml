name: Simone

on: [push]

jobs:
  Wizarding:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: vandijkstef/action-noticely@master
      with:
        token: ${{ secrets.GITHUB_TOKEN }}