name: Simone

on: [push]

jobs:
  Wizarding:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: vandijkstef/action-noticely
      with:
        token: ${{ secrets.GITHUB_TOKEN }}