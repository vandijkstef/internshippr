name: Simone

on: [push]

jobs:
  Wizarding:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./
      with:
        milliseconds: 500
        token: ${{ secrets.GITHUB_TOKEN }}